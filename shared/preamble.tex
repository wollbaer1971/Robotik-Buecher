% ============================================================
% shared/preamble.tex
% Zentrale Konfiguration für pdfLaTeX-Bände (Band 2–4)
% NICHT für Band 1 (LuaLaTeX) verwenden — siehe band1/main.tex
%
% Druckformat: Amazon KDP — 6,69 × 9,61 Zoll (17,0 × 24,4 cm)
% Compiler:    pdfLaTeX
% Einbinden:   \input{../shared/preamble} in band2–4/main.tex
% ============================================================

% ── KDP-Seitenformat ─────────────────────────────────────────
\usepackage[
  paperwidth  = 6.69in,
  paperheight = 9.61in,
  margin      = 20mm,
  headheight  = 14pt,
  headsep     = 8mm,
  footskip    = 10mm
]{geometry}

% ── pdfLaTeX: Schriften und Kodierung ────────────────────────
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[utf8]{inputenc}
\usepackage[ngerman]{babel}
\usepackage{csquotes}
\usepackage{microtype}

% ── Kopf- und Fußzeilen ──────────────────────────────────────
\usepackage{scrlayer-scrpage}
\clearpairofpagestyles
\ihead{\small\leftmark}
\ohead{\small\pagemark}
\pagestyle{scrheadings}

% ── Basis-Pakete ─────────────────────────────────────────────
\usepackage{xcolor}
\usepackage[most]{tcolorbox}
\tcbuselibrary{skins,breakable,listings}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{hyperref}

\setlist[itemize]{topsep=3pt,itemsep=2pt}
\setlist[enumerate]{topsep=3pt,itemsep=2pt}

% ── Farben ───────────────────────────────────────────────────
\definecolor{PythonBlau}{HTML}{1565C0}
\definecolor{PythonHellblau}{HTML}{E3F2FD}
\definecolor{PythonGruen}{HTML}{2E7D32}
\definecolor{PythonOrange}{HTML}{E65100}
\definecolor{CodeHintergrund}{HTML}{F5F5F5}
\definecolor{ROSRot}{HTML}{B71C1C}
\definecolor{ROSHell}{HTML}{FFEBEE}

% ── listings-Konfiguration ───────────────────────────────────
% footnotesize für KDP-Satzbreite (ca. 129 mm → ~72 Zeichen)
\lstdefinestyle{pythonstyle}{
  backgroundcolor = \color{CodeHintergrund},
  basicstyle      = \ttfamily\footnotesize,
  breaklines      = true,
  breakatwhitespace = false,
  keywordstyle    = \color{PythonBlau}\bfseries,
  stringstyle     = \color{PythonGruen},
  commentstyle    = \color{gray}\itshape,
  numberstyle     = \tiny\color{gray},
  numbers         = left,
  numbersep       = 5pt,
  showstringspaces= false,
  language        = Python,
  literate        =
    {ä}{{\"{a}}}1 {ö}{{\"{o}}}1 {ü}{{\"{u}}}1
    {Ä}{{\"{A}}}1 {Ö}{{\"{O}}}1 {Ü}{{\"{U}}}1
    {ß}{{\ss}}1
}

\lstdefinestyle{bashstyle}{
  backgroundcolor = \color{CodeHintergrund},
  basicstyle      = \ttfamily\footnotesize,
  breaklines      = true,
  breakatwhitespace = false,
  commentstyle    = \color{gray}\itshape,
  showstringspaces= false,
  literate        =
    {ä}{{\"{a}}}1 {ö}{{\"{o}}}1 {ü}{{\"{u}}}1
    {Ä}{{\"{A}}}1 {Ö}{{\"{O}}}1 {Ü}{{\"{U}}}1
    {ß}{{\ss}}1
}

\lstset{style=pythonstyle}

% ── Tcolorbox-Umgebungen ─────────────────────────────────────
\newtcolorbox{merksatz}[1][]{
  colback=PythonHellblau, colframe=PythonBlau,
  fonttitle=\sffamily\bfseries, title=Merksatz, #1}

\newtcolorbox{uebung}[1][]{
  colback=yellow!10, colframe=PythonOrange,
  fonttitle=\sffamily\bfseries, title=Übung, #1}

\newtcolorbox{praxis}[1][]{
  colback=green!8, colframe=PythonGruen,
  fonttitle=\sffamily\bfseries, title=Praxisbezug, #1}

\newtcolorbox{ros}[1][]{
  colback=ROSHell, colframe=ROSRot,
  fonttitle=\sffamily\bfseries, title=ROS2-Verbindung, #1}

\newtcolorbox{analogie}[1][]{
  colback=PythonHellblau, colframe=PythonBlau,
  fonttitle=\sffamily\bfseries, title=Analogie, #1}

\newtcolorbox{loesung}[1][]{
  colback=green!8, colframe=PythonGruen,
  fonttitle=\sffamily\bfseries, title=Schritt-für-Schritt-Lösung, #1}

% ── hyperref (Druck: neutrale Farben) ────────────────────────
\hypersetup{
  colorlinks = true,
  linkcolor  = black,
  urlcolor   = black,
  citecolor  = black
}

% ── TOC-Breite ───────────────────────────────────────────────
\DeclareTOCStyleEntry[numberwidth=3em]{tocline}{part}

% ── Band-übergreifende Variablen ─────────────────────────────
\newcommand{\BuchReihe}{Robotik entwickeln lernen}
\newcommand{\BandAutor}{Wolfgang Becker}
\newcommand{\BandVersion}{Erste Auflage}
